# 🚀 Запуск FinvizPro локально

## Быстрый старт

### Вариант 1: Использование ярлыка (рекомендуется)
1. Найдите ярлык **FinvizPro** на рабочем столе
2. Дважды кликните по нему
3. Подождите 10-15 секунд
4. Браузер автоматически откроется с приложением

### Вариант 2: Запуск через bat-файл
Запустите один из файлов в папке проекта:
- **`start_finvizpro.bat`** - с проверкой доступности сервера (требует curl)
- **`start_finvizpro_simple.bat`** - простой вариант (ждет 10 секунд)

## Первый запуск

При первом запуске необходимо установить зависимости:

```bash
cd backend
pip install -r requirements.txt
```

Или просто запустите `start_finvizpro.bat` - он автоматически установит зависимости, если их нет.

## Что происходит при запуске?

1. ✅ Проверяется наличие Python
2. ✅ Проверяются и устанавливаются зависимости (если нужно)
3. ✅ Запускается бэкенд Flask на порту 5000
4. ✅ Открывается браузер с адресом http://localhost:5000

## Остановка приложения

Закройте окно терминала с названием **"FinvizPro Backend"**

## Устранение проблем

### Ошибка "ERR_CONNECTION_REFUSED"

**Причина:** Бэкенд не успел запуститься или произошла ошибка при запуске.

**Решение:**
1. Проверьте окно **"FinvizPro Backend"** на наличие ошибок
2. Убедитесь, что все зависимости установлены:
   ```bash
   cd backend
   pip install -r requirements.txt
   ```
3. Попробуйте запустить бэкенд вручную:
   ```bash
   cd backend
   python app.py
   ```
4. Если появляются ошибки, убедитесь, что установлен Python 3.7+

### Порт 5000 уже занят

**Решение:**
1. Закройте все программы, использующие порт 5000
2. Или измените порт в файле `backend/app.py` (последняя строка)

### Python не найден

**Решение:**
1. Установите Python с официального сайта: https://www.python.org/downloads/
2. При установке отметьте "Add Python to PATH"
3. Перезапустите компьютер

## Ручной запуск (для разработчиков)

### Запуск только бэкенда:
```bash
cd backend
python app.py
```

Бэкенд будет доступен по адресу: http://localhost:5000

### Доступные эндпоинты:
- `GET /` - информация об API
- `GET /api/quote/<ticker>` - данные по акции
- `GET /api/quote/<ticker>/chart` - график акции
- `GET /api/quote/<ticker>/news` - новости по акции
- `GET /api/quote/<ticker>/insider` - инсайдерские сделки
- `GET /api/screener` - скринер акций
- `GET /api/news` - общие новости
- `GET /api/insider` - общие инсайдерские сделки

## Требования

- Python 3.7+
- pip (менеджер пакетов Python)
- Интернет-соединение (для получения данных с Finviz)

## Зависимости

- finvizfinance - библиотека для работы с Finviz
- flask - веб-фреймворк
- flask-cors - поддержка CORS
- pandas - обработка данных
- gunicorn - WSGI сервер (для продакшена)
